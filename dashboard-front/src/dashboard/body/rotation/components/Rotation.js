import React  from 'react';
import PropTypes from 'prop-types';

const Rotation = ({ classes }) => (
	<div>Rotation</div>
);

Rotation.propTypes = {
	classes: PropTypes.object.isRequired,
};

export default Rotation;
